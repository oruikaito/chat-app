<div class="side_bar_header">
  <div class="user_name">
    <a href="#"><%= link_to current_user.name, edit_user_path(current_user) %></a>
  </div>
  <div class="chat_create_buttom">
    <%= link_to "チャットを作成する", new_room_path %>
  </div>
</div>

<div class="rooms">
  <% current_user.rooms.each do |room| %>
  <div class="room">
    <div class="room_name">
      <a href="#"><%= room.name %></a>
    </div>
  </div>
  <% end %>
</div>